<?php
    $this->plugin('basePath')->setBasePath('/public/');

    $this->headMeta()
                    ->setProperty("og:url", "http://ospensadores.com")
                    ->setProperty("og:title", "Pra quem acredita na vida, no amor e em Deus")
                    ->setProperty("og:description", "Pense, só isso, pense, não queremos mais nada além disso: que você pense e pense bem, com sua própria cabeça, queremos que você tenha opinião própria, uma vida com objetivos, cheia de amor e cheia de Deus.")
                    ->setProperty("og:image", 'http://www.ospensadores.com/public/img/divulgacao/post-inst-redes.png')
                    ->setProperty("og:image:type", "png")
                    ->setProperty("og:image:width", "400")
                    ->setProperty("og:image:height", "400")
                    ->setProperty("og:type", "website");

    $ultimas_colunas = $this->ultimas_colunas; 
    $colunas = $this->colunas;
?>

<style>
    .bloco {
        margin-top: 15px;
    }
    
    .bloco-destaque {
        background: white;
        /*border-top: 3px solid #D89D33;*/
        margin-right: 15px;
        padding: 0;
    }

    .bloco-titulo {
        font-size: 18px; 
        line-height: 25px;
    }
    
    .icone-categoria{ margin-top: 2px; }
    
    .bjqs-caption{
        font-size: 18px; 
        line-height: 25px;
        text-align: left;
        color: white
    }
    
</style>

<!--<div id="slider" class="bloco bloco-destaque col-md-9">
    
    <div style="background: linear-gradient(to right, #D89D33, #C270AD, #2D84C6); height: 4px"></div>
    
    <ul class="bjqs">
        <?php
            foreach ($ultimas_colunas as $coluna) {
                //echo $this->partial('bloco/destaque',['coluna'=>$coluna]);
            }
        ?>
    </ul>
</div>-->

    <?php
        foreach ($ultimas_colunas as $coluna) {
            echo $this->partial('bloco/comum',['coluna'=>$coluna]);
        }
    ?>
    <?php
        foreach ($colunas as $coluna) {
            echo $this->partial('bloco/comum',['coluna'=>$coluna]);
        }
    ?>

<script>
    $('#slider').bjqs({
        height      : 485,
        width       : 620,
        responsive  : true,
        automatic   : false,
        showcontrols: false,
        showmarkers : false,
        animspeed   : 3000, //1000 = 1s
        //ref: http://www.basic-slider.com/
      });
</script>